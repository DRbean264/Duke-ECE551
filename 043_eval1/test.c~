#include <stdio.h>
#include <stdlib.h>

int main()
{
    char *testStr = "--342";
    char *leftover;
    unsigned long result = strtoul(testStr, &leftover, 10);
    printf("%lu\n", result);
    return 0;
}
